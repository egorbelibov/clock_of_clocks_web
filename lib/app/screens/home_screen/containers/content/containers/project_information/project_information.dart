import 'package:flutter/widgets.dart';

/// Introduction to Project and Action
class ProjectInformation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}
