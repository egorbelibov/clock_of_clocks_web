name: clock_of_clocks_website
description: Clock of clocks website project.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  property_change_notifier: ^0.2.0

  # For clock_of_clocks (integration):
  intl: ^0.16.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
   - assets/images/clock_table.png
   - assets/images/clock_table_dark.png
   - assets/images/lenovo_clock.png
   - assets/images/lenovo_clock_dark.png